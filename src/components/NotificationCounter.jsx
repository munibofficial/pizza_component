import React from "react";
import NotificationList from "./NotificationList";

const NotificationCounter = ({count})=>{
    return <span className="notification-counter">{count}</span>

};

export default NotificationCounter;